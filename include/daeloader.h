#pragma once

#include <stdio.h>
#include "object3d.h"
#include <utility>
#include <vector>

class DAELoader {
    public:
    static std::pair<std::vector<object3d>::iterator, std::vector<object3d>::iterator> DAELoader::load(char path[]) {
        
    }
};